name: main

on:
  push:
    branches:
      - master
      - ci

- uses: convictional/trigger-workflow-and-wait@v1.6.1
  with:
    owner: platform-os
    repo: gp-style-guide
    github_token: ${{ secrets.GITHUB_PERSONAL_ACCESS_TOKEN }}
